# models package — data models for the application
# This __init__.py makes 'models' a Python package

# Absolute imports — importing classes from student module
from models.student import Person, Student, GraduateStudent
